# Phase 5 Debugging Report — Pets & Edge Features / Final Verification

**Repo:** `/home/bob/Documents/2140wtf/ditto`  
**Date:** 2026-06-18  
**Baseline:** 28 test files passed, 469 tests passed, 0 ESLint warnings  
**Final:** 29 test files passed, 479 tests passed, 0 ESLint warnings, `vite build` succeeds

> Note: the working tree already contained extensive uncommitted changes when Phase 5 started (principally the Blobbi → Pets rename and related refactoring). This report covers only the clearly-correct fixes applied during Phase 5.

---

## Part A — Pets correctness & gating

### Files reviewed

- `src/pets/actions/lib/evolution-missions.ts` — static mission definitions are valid, no divide-by-zero/off-by-one.
- `src/pets/actions/hooks/useHatchTasks.ts` / `useEvolveTasks.ts` — empty/malformed `evolution[]` handled gracefully via `findEvolutionMission` fallbacks; `parseEvolutionContent` never throws.
- `src/pets/actions/hooks/usePetsIncubation.ts` — switch-mode stat parsing hardened.
- `src/pets/actions/hooks/usePetsStageTransition.ts` — publish only happens after tag validation; errors throw before any event is published (natural rollback).
- `src/pets/actions/lib/daily-mission-tracker.ts` — session store updates are synchronous per call; `hydrateEvolutionFromPersisted` guards against overwriting existing store entries.
- `src/pets/core/lib/pets.ts` / `src/pets/core/lib/missions.ts` — malformed JSON or mission arrays parse to safe defaults.
- `src/pets/companion/components/PetsCompanion.tsx` — no crash on missing optional state; animation frame cleaned up on unmount.
- `src/pages/PetsPage.tsx` — ceremony decision tree guards against empty egg arrays.
- `src/lib/sidebarItems.tsx` / `src/lib/sidebarWidgets.ts` / `src/AppRouter.tsx` — no Pets route/menu/widget exposed; `/pets` redirects to `/`.

### Bugs fixed

| File | Problem | Fix |
|------|---------|-----|
| `src/pets/actions/hooks/usePetsIncubation.ts` | Switch-mode stat parser used raw `parseInt` with `?? '50'` fallback, so malformed non-numeric tag values (e.g. `"abc"`) produced `NaN` stats and propagated through decay. | Replaced inline parser with `getNumericTag`, which falls back to the default when the tag is missing **or** `parseInt` returns `NaN`. |

### Remaining disabled / exposed Pets wiring

- **Sidebar:** no Pets item in `src/lib/sidebarItems.tsx`.
- **Widgets:** no Pets widget in `src/lib/sidebarWidgets.ts`.
- **Router:** `/pets` is explicitly redirected to `/` in `src/AppRouter.tsx`; the lazy `PetsPage` import is present but unreachable via the app shell.
- **Internal code:** The Pets feature is still compiled in (lazy route would resolve if a user manually navigated to `/pets` before the redirect, but the redirect prevents that). This matches the stated intent: "Pets is currently disabled/dead until designs are ready, but we still want the code to be correct and safely gated."

---

## Part B — Edge features final sweep

### Files reviewed

- `src/pages/WebxdcFeedPage.tsx` / `src/hooks/useWebxdc.ts` / `src/components/WebxdcEmbed.tsx` — mini-apps rebrand is complete; user-facing strings use "Mini app".
- `src/components/Webxdc.tsx` — not in this phase's target list; unhandled `.then()` promises inside the sandboxed bridge script were left as-is per Phase 1 rationale.
- `src/pages/WikipediaPage.tsx` / `src/pages/TrendsPage.tsx` / `src/pages/EventsFeedPage.tsx` / `src/pages/BooksPage.tsx` / `src/pages/BadgesPage.tsx` / `src/pages/WorldPage.tsx` / `src/components/LiveStreamPage.tsx` / `src/pages/StreamsFeedPage.tsx` — empty/error/loading states handled.
- `src/components/ExternalContentHeader.tsx` / `src/components/AppHandlerContent.tsx` / `src/components/SandboxFrame.tsx` — sandboxing and URL sanitization in place; iframe `sandbox`/`allow` attributes omit unsafe capabilities.

### Bugs fixed

| File | Problem | Fix |
|------|---------|-----|
| `src/hooks/useWebxdc.ts` | Used `mutate` from `useNostrPublish`, which returns `void`. `publishSigned(...).then(...)` therefore threw for logged-in users, breaking mini-app state updates and realtime sends. | Switched to `mutateAsync` and returned/awaited the publish promise in both the logged-in and logged-out branches so callers can chain and catch errors correctly. |
| `src/components/LiveStreamPage.tsx` | `formatDateTime(parseInt(starts))` could render "Invalid Date" if the `starts` tag was non-numeric; also missing radix. | Guarded with `!Number.isNaN(parseInt(starts, 10))` and added explicit radix `10`. |

---

## Part C — Final verification

```bash
npx tsc --noEmit          # ✅ clean
npx eslint --no-cache .   # ✅ 0 errors, 0 warnings
npx vitest run --reporter=dot --silent
# Test Files  29 passed (29)
#      Tests  479 passed (479)
npx vite build -l error   # ✅
```

### New tests

- `src/pets/core/lib/missions.test.ts` — 10 tests covering `parseEvolutionContent` and `parseProfileContent` malformed/empty/valid handling.

---

## High-level summary of all 5 phases

| Phase | Focus | Bugs fixed | Tests added | Final test count |
|-------|-------|------------|-------------|------------------|
| 1 | Unhandled promises, localStorage safety | 10 | 0 | 436 passed / 24 files |
| 2 | *(report not found in repo)* | — | — | — |
| 3 | *(report not found in repo)* | — | — | — |
| 4 | Wallets & payments | 10 | 26 | 469 passed / 28 files |
| 5 | Pets gating/correctness + edge features + final verification | 3 | 10 | **479 passed / 29 files** |

**Verified totals from present reports:** 23+ bugs fixed, 36+ tests added across Phases 1, 4, and 5. Final state: **479 tests passing, 29 test files passing, 0 ESLint warnings, TypeScript clean, Vite build succeeds.**

No `git commit` / `git push` performed.
